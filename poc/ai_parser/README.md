# AI Parser PoC

本資料夾預留給 AI 萃取 action item、owner、due date、risk、decision 與 KM candidate 的 PoC。

初期目標：

1. 讀取標準 JSON event。
2. 產生 AI draft extraction。
3. 標記 `needs_human_review`。
4. 輸出 Markdown、SQLite 或 HTML dashboard 所需資料。

